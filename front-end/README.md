# SPG Frontend

Cloudflare Pages: Root directory `front-end`; build command `npm run build`; output `dist`.

Set `VITE_API_URL` to the Render backend URL and `VITE_LOGO_URL` to the Cloudinary logo URL.